# Python requirements for AD Prediction Pipeline
# Adapted from Paper 1's requirement.txt
# CUDA 12.3 compatible

# Core LLM inference — install vllm first, it pulls compatible torch
vllm==0.5.5

# Data processing (same as original)
jsonlines
rich
fraction
sympy==1.12
multiprocess
backoff
ray

# Additional packages for AD pipeline
pandas>=2.0.0
numpy>=1.24.0
tqdm
transformers>=4.35.0
scikit-learn>=1.3.0
